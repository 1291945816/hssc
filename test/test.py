import operator

if __name__ == '__main__':
    # comp_cost = [9, 2, 16, 4]
    # p, w = min(enumerate(comp_cost), key=operator.itemgetter(1))
    # p, w = min(enumerate(comp_cost), key=lambda e_i: e_i[1])
    # print(f"{p} {w}")

    aft = float('-inf')

    print(aft < 1)
